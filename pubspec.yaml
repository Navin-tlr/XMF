name: smart_calorie_flutter
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.15.1
  firebase_auth: ^4.9.0
  cloud_firestore: ^4.9.1
  flutter_riverpod: ^2.3.7
  rxdart: ^0.27.7
  google_fonts: ^5.1.0
  intl: ^0.18.1
  percent_indicator: ^4.2.3 # <-- ADDED

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
